import { makeStyles, createStyles } from '@material-ui/core';

export const useStyles = makeStyles(createStyles({
  muiSlider: {
    padding: '4px 0px'
  },
  typography: {
    fontSize: '0.8rem'
  }
}));
